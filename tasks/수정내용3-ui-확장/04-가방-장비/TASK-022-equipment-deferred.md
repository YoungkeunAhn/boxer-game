# TASK-022 가방·장비 시스템 (P4 — 보류 골격)

> ⏸ **보류(P4).** 2026-06-21 결정으로 **장비·훈련 시스템은 구현하지 않는다.** TASK-016에서 강화를 현행 9종으로 유지(장비강화 축 미채택)했으므로 02 파이터와의 강제 커플링은 해소됐고, 본 파트는 독립적으로 보류 가능하다. 이 문서는 **P4 착수 결정 시 출발점**으로만 유지한다. 현재 코드 변경 없음.

## 보류 사유

- `docs/기획/overview/mvp-scope.md`에서 **장비 명시적 제외**.
- 장비 능력치 합산은 `calculateCombatStats`를 전면 변경 → 전투/콤비네이션 테스트 파급 + `BALANCE_VERSION`.
- 인벤토리/착용 상태는 대형 저장 구조 → `SCHEMA_VERSION` 큰 변경, 타입가드 다수.

## 도입(P4) 착수 조건

- P3(재화·레벨·퀘스트)가 안정화되고 다이아 sink·성장 곡선이 확정된 뒤.
- 장비가 더하는 능력치의 `calculateCombatStats` 합산 위치·`BALANCE_VERSION` 영향 검토 완료.
- mvp-scope에서 장비 제외 해제 결정.

## 도입 시 채울 섹션 (참고: `docs/ui/04-가방-장비.md` §3)

- [ ] 아이템 데이터 모델: 타입 4분류, 슬롯 종류, 등급(레어도)·색·옵션 범위, 속성(id/슬롯/등급/레벨/+N/옵션/스택).
- [ ] 능력치 반영: 장비 옵션의 `calculateCombatStats` 합산 방식, 글로벌 "장비강화" 배수 vs 개별 강화 관계.
- [ ] 강화/획득: 비용·성공률·상한·파괴 여부(MVP는 100%·비파괴 권장), 획득처(드랍/상자/제작), 인벤토리 상한·확장·정렬.
- [ ] UI: 슬롯-그리드-상세 상호작용, 360px 배치.
- [ ] 데이터 모델 초안(`EquipmentItem`/`Inventory`)은 `docs/ui/04-가방-장비.md` §3-5 참조.

## 커플링 플래그(도입 시)

- 🔴 MVP 밖(장비) 해제 + 능력치 수식 전면 영향 → `BALANCE_VERSION`.
- 🔴 인벤토리/착용 = 대형 저장 구조 → `types.ts`/`save.ts`/`SCHEMA_VERSION` 큰 변경.
- 🟢 02 파이터가 9종 유지로 강제 커플링 해소 — 독립 도입 가능.

## 현재 완료 기준

- **구현 없음.** 보류 결정과 도입 조건이 문서로 명확히 유지되면 충족.
